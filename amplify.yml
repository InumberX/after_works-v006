version: 1
applications:
  - appRoot: src/app/frontend
    frontend:
      phases:
        preTest:
          commands:
            - yarn install
        test:
          commands:
            - yarn prettier
            - yarn stylelint
            - yarn lint:quiet
        preBuild:
          commands:
            - yarn install
        build:
          commands:
            - yarn start
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
